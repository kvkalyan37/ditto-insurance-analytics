name: Deploy to Kind (Local Testing)

on:
  workflow_dispatch:
  push:
    branches:
      - main
      - master
    paths:
      - '.github/workflows/deploy-kind.yml'

env:
  DOCKERHUB_USERNAME: ${{ secrets.DOCKERHUB_USERNAME }}
  DOCKERHUB_TOKEN: ${{ secrets.DOCKERHUB_TOKEN }}

jobs:
  deploy-to-kind:
    name: Deploy to Kind Cluster
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Generate timestamp tag
        id: timestamp
        run: |
          TIMESTAMP=$(date +%Y%m%d-%H%M%S)
          echo "timestamp=$TIMESTAMP" >> $GITHUB_OUTPUT
          echo "🕐 Generated timestamp: $TIMESTAMP"

      - name: Build Frontend image
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          file: ./frontend/Dockerfile
          push: false
          tags: ditto-frontend:${{ steps.timestamp.outputs.timestamp }}
          load: true
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/ditto-frontend:${{ steps.timestamp.outputs.timestamp }}

      - name: Build API image
        uses: docker/build-push-action@v5
        with:
          context: ./api_service
          file: ./api_service/Dockerfile
          push: false
          tags: ditto-api:${{ steps.timestamp.outputs.timestamp }}
          load: true
          cache-from: type=registry,ref=${{ secrets.DOCKERHUB_USERNAME }}/ditto-api:${{ steps.timestamp.outputs.timestamp }}

      - name: Set up Kind
        uses: engineerd/setup-kind@v0.6.0
        with:
          version: "v0.20.0"

      - name: Create Kind cluster
        run: |
          kind create cluster --name ditto-insurance --wait 5m || true
          kind get kubeconfig --name ditto-insurance > $HOME/.kube/config

      - name: Load images into Kind
        run: |
          kind load docker-image ditto-frontend:${{ steps.timestamp.outputs.timestamp }} --name ditto-insurance
          kind load docker-image ditto-api:${{ steps.timestamp.outputs.timestamp }} --name ditto-insurance

      - name: Set up Helm
        uses: azure/setup-helm@v3

      - name: Deploy with Helm (Helm-only deployment)
        run: |
          # Deploy using Helm exclusively - no direct kubectl apply/manifest deployments
          helm upgrade --install ditto-insurance ./helm/ditto-insurance \
            --namespace ditto-insurance \
            --create-namespace \
            --set frontend.image.repository=ditto-frontend \
            --set frontend.image.tag=${{ steps.timestamp.outputs.timestamp }} \
            --set api.image.repository=ditto-api \
            --set api.image.tag=${{ steps.timestamp.outputs.timestamp }} \
            --wait \
            --timeout 5m

      - name: Verify deployment
        run: |
          kubectl get pods -n ditto-insurance
          kubectl rollout status deployment/ditto-insurance-frontend -n ditto-insurance --timeout=3m
          kubectl rollout status deployment/ditto-insurance-api -n ditto-insurance --timeout=3m

